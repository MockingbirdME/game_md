extends layout

block content
    div
        h2 Documentation Links:
        ul
            each documentName in availableDocumentation
                li
                    a(class='document-link' href=`/documentation/${documentName}`)=documentName
